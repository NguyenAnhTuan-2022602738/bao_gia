<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Báo Giá Đồ Cao Su - Quang Minh</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold mb-4 text-center">BÁO GIÁ ĐỒ CAO SU - QUANG MINH</h1>
        <div class="flex flex-wrap justify-around mb-8 p-4 bg-gray-50 rounded-lg">
            <% qrCodes.forEach(qr => { %>
                <div class="flex flex-col items-center p-4">
                    <h3 class="mb-2 font-medium"><%= qr.label %></h3>
                    <img src="<%= qr.qrCode %>" alt="QR Code <%= qr.label %>">
                    <p class="mt-2 text-sm text-gray-600">Quét để xem giá <%= qr.label %></p>
                </div>
            <% }) %>
        </div>
        <div class="text-center">
            <p>Quét QR code tương ứng để xem báo giá</p>
        </div>
    </div>
</body>
</html>